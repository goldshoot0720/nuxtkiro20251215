# 🎬 Netlify Blobs 影片設置指南

## 📋 概述

這個指南將幫助你設置 Netlify Blobs 來存儲和提供影片內容。Netlify Blobs 是一個高性能的對象存儲服務，非常適合存儲大型媒體文件。

## 🚀 快速設置

### 1. 啟用 Netlify Blobs

在 Netlify 控制台中：
1. 進入你的網站設置
2. 找到 "Blobs" 部分
3. 啟用 Blobs 功能

### 2. 安裝依賴

```bash
npm install @netlify/blobs
```

### 3. 上傳影片

#### 方法一：使用腳本上傳

1. 將影片文件放在 `public/videos/` 資料夾中：
   ```
   public/videos/
   ├── 19700121-1829-693fee512bec81918cbfd484c6a5ba8f_enx4rsS0.mp4
   └── clideo-editor-92eb6755d77b4603a482c25764865a58_7sLjgTgc.mp4
   ```

2. 運行上傳腳本：
   ```bash
   npm run upload-videos
   ```

#### 方法二：使用 Netlify CLI

1. 安裝 Netlify CLI：
   ```bash
   npm install -g netlify-cli
   ```

2. 登入並連結專案：
   ```bash
   netlify login
   netlify link
   ```

3. 上傳單個影片：
   ```bash
   netlify blobs:set videos "19700121-1829-693fee512bec81918cbfd484c6a5ba8f_enx4rsS0.mp4" "./public/videos/19700121-1829-693fee512bec81918cbfd484c6a5ba8f_enx4rsS0.mp4"
   ```

#### 方法三：手動上傳（Netlify 控制台）

1. 進入 Netlify 控制台
2. 選擇你的網站
3. 進入 "Blobs" 部分
4. 創建 "videos" store
5. 手動上傳影片文件

## 🔍 檢查狀態

### 在網站中檢查

1. 進入影片介紹頁面
2. 點擊 "🔍 檢查 Netlify Blobs" 按鈕
3. 查看每個影片的狀態

### 使用腳本檢查

```bash
npm run check-blobs
```

### 測試連接

```bash
npm run test-blobs
```

## 📁 文件結構

```
project/
├── netlify/
│   └── functions/
│       └── blob-proxy.js          # Netlify Blobs 代理函數
├── scripts/
│   ├── upload-videos.js           # 上傳腳本
│   ├── check-blobs.js            # 檢查狀態腳本
│   └── test-blobs-connection.js   # 測試連接腳本
├── public/
│   └── videos/                    # 本地影片文件（上傳前）
│       ├── video1.mp4
│       └── video2.mp4
└── app/
    └── app.vue                    # 主應用（包含影片播放器）
```

## 🎯 影片配置

在 `app/app.vue` 中的影片列表：

```javascript
const videoList = ref([
  {
    blobKey: '19700121-1829-693fee512bec81918cbfd484c6a5ba8f_enx4rsS0.mp4',
    displayName: '鋒兄的傳奇人生',
    fileSize: 15728640,
    uploadedAt: '2025-01-21',
    // ... 其他屬性
  },
  // ... 更多影片
])
```

## 🔧 故障排除

### 問題：影片無法載入

**解決方案：**
1. 檢查 Netlify Blobs 狀態
2. 確認影片已正確上傳
3. 檢查 blob-proxy.js 函數是否正常運行

### 問題：上傳腳本失敗

**可能原因：**
- `@netlify/blobs` 未安裝
- Netlify CLI 未登入
- 影片文件不存在
- 網路連接問題

**解決方案：**
```bash
# 重新安裝依賴
npm install @netlify/blobs

# 檢查 Netlify CLI 狀態
netlify status

# 重新登入
netlify login
```

### 問題：本地開發環境無法訪問 Blobs

**解決方案：**
使用 Netlify Dev 環境：
```bash
netlify dev
```

## 📊 性能優化

### 1. 快取策略

Netlify Blobs 代理函數已配置快取：
```javascript
'Cache-Control': 'public, max-age=86400'  // 24小時快取
```

### 2. 壓縮

影片文件建議使用 H.264 編碼和適當的壓縮設置。

### 3. 預載入

使用應用內的快取功能來預載入常用影片。

## 🔐 安全性

### 訪問控制

目前配置為公開訪問。如需私有訪問，可以：
1. 修改 blob-proxy.js 添加認證
2. 使用 Netlify 的身份驗證功能
3. 實現自定義訪問控制邏輯

### CORS 設置

已在 blob-proxy.js 中配置：
```javascript
'Access-Control-Allow-Origin': '*'
```

## 📈 監控

### 使用統計

在 Netlify 控制台中查看：
- Blobs 使用量
- 請求統計
- 頻寬使用

### 應用內監控

影片介紹頁面提供：
- 即時狀態檢查
- 快取統計
- 錯誤報告

## 🎉 完成檢查清單

- [ ] Netlify Blobs 已啟用
- [ ] 依賴已安裝
- [ ] 影片已上傳到 Blobs
- [ ] 狀態檢查通過
- [ ] 影片可以正常播放
- [ ] 快取功能正常
- [ ] 錯誤處理正常

---

**需要幫助？** 查看 [Netlify Blobs 官方文檔](https://docs.netlify.com/blobs/overview/) 或檢查應用內的狀態信息。